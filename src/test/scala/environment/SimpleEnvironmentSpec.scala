package environment

import scala.reflect.runtime.universe._
import environment.NamespaceTypes.{TypedValue}
import org.scalatest.{FlatSpec, Matchers}

class SimpleEnvironmentSpec extends FlatSpec with Matchers {

  "An Environment" should "find functions by name and matching type" in {
//    val boolVariable = TypedValue("variable", true)
//    val intVariable = TypedValue("variable", 1)
//    val environment = new Environment(List(
//      boolVariable
//    ))
//
//    println(environment.resolveVariable("variable", typeOf[Boolean]))

  }

}
